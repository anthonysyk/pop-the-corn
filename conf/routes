# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET        /                       controllers.HomeController.index

GET        /search                 controllers.SearchController.searchMovie(q: String)

GET        /enrich                 controllers.EnricherController.enrichMovies

GET        /enrichBlocking         controllers.EnricherController.enrichBlocking


GET        /startEnrichment        controllers.EnricherController.startEnrichment

GET        /startIndexing          controllers.EnricherController.startIndexing

GET        /countMovies            controllers.SearchController.countMovies

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file           controllers.Assets.versioned(path="/public", file: Asset)